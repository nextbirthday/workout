package b3_1213;

public class Notepad {
    // Table = row + column
    
    // event protocol
    // event src (button, check box, textField)에 대응하는 interface를 찾아라.
    // abstract method를 찾아서 재정의해야 한다.
    
    // event source와 event 처리를 담당하는 handler class를 매칭함
    // button.addActionListener(this);
    
    // actionPerformed method overriding (재정의)
    // 구현체 class가 있어야한다.
    
    // ActionListener - Interface
}
