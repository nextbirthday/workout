package b3_1214;

public class Notepad {
    // field - global variable location
    // 선언에서 결정되는 것은 무엇? type과 variable名
    // primitive type 에서는 NullPointerException이 발생할 수 없다. (Never happened)
    // primitive type은 field와 method를 가질 수 없다.(명사형과 동사형을 가질 수 없다.)
    
    // reference type은 address 값을 가지고 있다.
    // sub.insert(); 이렇게 method를 할 때 sub가 인스턴스화가 되어 있어야 한다.
    // null이면 memory에 상주하고 있는 내용이 아무것도 없다.
    
    // default constructor은 생략이 가능하다.
    // parameter가 있는 constructor는 생략이 불가하다.
    // parameter가 있는 constructor가 하나라도 있으면 default constructor도 생략이 불가하다.
    // instance화 할 때 생성부의 이름으로 객체가 생성된다.
    // 이 때 파라미터 자리에 인자를 추가할 수 있다. -여러개의 생성자를 선언할 수 있다.
    // = method overloading rule
    // parameter의 개수가 다르거나 type가 달라야 한다.
    
    
}
