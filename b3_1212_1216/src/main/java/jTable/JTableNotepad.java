package jTable;

public class JTableNotepad {
    // ActionListener al = new DepartmentTable03(); 선언부와 생성부가 다를 수 있다. 이럴때만 다형성이 가능하다.
    // 선언부와 생성부가 다르다. 동일한 method를 호출했는데 다른 기능이 처리되었다?
    // 생성부의 이름으로 객체가 만들어진다.
    // abstract class도 구현체 클래스가 필요하다. - this, constructor
    // Duck myDuck = new WoodDuck(); myDuck.fly();
    // Duck herDuck = new RubberDuck(); herDuck.fly();
    // Duck himDuck = new MallardDuck(); himDuck.fly();
    
    // Vector - First In First Out
    // Call stack - First In Last Out
}
