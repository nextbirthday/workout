package java_b3_1202;

public class Notepad {
    /*
     * [원시형 변수]
     * 
     * 원시형 타입은 변수와 메서드를 가질 수 없다. why?클래스가 아니니까 소유할 수 없다.
     * 
     * 원시형 타입은 참조형 타입과는 다르게 호출하면 값이 나오므로 간접 참조 방식으로 사용할 수 없다.
     * 
     * 원시형 변수는 참조형이 아니므로 변수와 메서드를 소유할 수 없다. 선언할 수 없다. 가질 수 없다.
     * 
     * 원시형 타입은 '값'이 출력되니까 변수와 메서드를 선언할 수 없다.
     * 
     * 원시형 변수는 직접 참조 방식이므로 값이 호출된다.
     * 
     * [참조형 변수]
     * 
     * 참조형 타입은 간접 참조 방식이므로 변수나 메서드를 가질 수 있다. 선언할 수 있다. 구현할 수 있다.
     */
    
}
